<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.omnistreamforce</groupId>
        <artifactId>omnistreamforce-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>omnistreamforce-cli</artifactId>
    <packaging>jar</packaging>
    <name>OmniStreamForce CLI</name>

    <dependencies>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-core</artifactId>
        </dependency>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-ai</artifactId>
        </dependency>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-domain-healthcare</artifactId>
        </dependency>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-domain-ecommerce</artifactId>
        </dependency>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-domain-energy</artifactId>
        </dependency>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-domain-autos</artifactId>
        </dependency>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-domain-highway</artifactId>
        </dependency>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-domain-fastfood</artifactId>
        </dependency>
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-web</artifactId>
        </dependency>
        <!-- sink de base de datos: permite elegir destino DB_OUTBOX o DUAL desde el CLI -->
        <dependency>
            <groupId>de.omnistreamforce</groupId>
            <artifactId>omnistreamforce-persistence</artifactId>
        </dependency>

        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
        </dependency>
    </dependencies>

    <build>
        <finalName>omnistreamforce-cli</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>info.picocli</groupId>
                            <artifactId>picocli-codegen</artifactId>
                            <version>${picocli.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-Aproject=${project.groupId}/${project.artifactId}</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>de.omnistreamforce.cli.OmniStreamForceApp</mainClass>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>META-INF/maven/**</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>